.env
.mo/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.coverage
logs/
memory/
config.yaml
donation_site/
fun/
*.bat
!mo.bat
hello.py
calc.py
mario_game/
.tmp/
tmp/
nul
with open*
personal/
.claude/
# Operator-private lanes. `devmode/` is TRACKED in private rMO (the protocol
# pack + maintainer guide + operator context, for operator/agents/devs) but is
# kept out of the public IQMO/MO snapshot via .gitattributes export-ignore.
# The bulky session records / archives below stay local-only.
docs/devmode/
docs/comparisons/
docs/.archived/
