# MO core package
# Subpackage structure:
#   core/agent/       — agent core, DNA, PRT, slash, turn, helpers
#   core/context/     — context builders, work patterns, workspace awareness
#   core/diagnostics/ — doctor and deterministic health reports
#   core/gates/       — input gates, final gates, safety scans, consistency boundary
#   core/ghost/       — ghost proposal and desktop context support
#   core/goal/        — goal runner and auditor
#   core/graph/       — code graph and structural graph
#   core/learning/    — learning modules and knowledge stores
#   core/profile/     — user/project profile model
#   core/prompts/     — runtime system prompt assets and loaders
#   core/provider/    — LLM provider, audit, model slots, capacity
#   core/review/      — critic, diff review, scorer, fix loop, finding patterns
#   core/runtime/     — monitor, heartbeat, locks, scheduler, daemon service
#   core/session/     — session management, handoff, closeout, momentum
#   core/skills/      — local skill packs and shipped seed packs
#   core/state/       — profile-home paths, initialization, secrets, migration
#   core/tasking/     — task board, task manager, task evidence, contract gate
#   core/telegram/    — Telegram gateway, sessions, auth, auto-replies
#   core/tooling/     — tool sandbox, registry, compression, file ledgers
#   core/utils/       — low-level shared helpers
#   core/worker/      — worker registry, runtime, routing policy
