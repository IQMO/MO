[pytest]
testpaths = tests
norecursedirs = tmp .git __pycache__ .pytest_cache .ruff_cache logs memory .omx operator
markers =
    smoke: fast core verification (<3KB test files, typically <5s)
    unit: standard unit tests (3-15KB, typically <30s)
    full: comprehensive tests (>15KB, may be slow)
