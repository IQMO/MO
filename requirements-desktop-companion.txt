# Optional: desktop companion surface (on-screen text-input window + global hotkey).
# The companion is a MO-branded minimal GUI that runs alongside the TUI.
# All of them degrade gracefully to a clear error if the matching package is
# absent, so the core agent runs fine without these.
#
#   companion window            -> tkinter (built into Python)
#   global hotkey (Win+Alt+M)   -> keyboard (local desktop only; needs a display)
#   system tray icon + menu     -> pystray + Pillow (icon image)
#   Windows tray/overlay glue   -> pywin32 (Windows only)
keyboard>=0.13.5
pystray>=0.19
Pillow>=10.0
pywin32>=306; sys_platform == "win32"

# Optional: voice (Phase 3) — local speech-to-text + text-to-speech
#   speech-to-text (STT)        -> faster-whisper (CTranslate2-backed Whisper)
#   text-to-speech (TTS)        -> piper-tts (neural TTS with espeak-ng)
#   audio capture/playback      -> sounddevice (cross-platform PortAudio wrapper)
#   audio array handling        -> numpy
faster-whisper>=1.0.0
piper-tts>=1.2.0
sounddevice>=0.4.6
numpy>=1.24
